export interface PuppiesProps {
    puppyImage: string;
    puppyName: string;
    puppyDesc: string;
    puppyAbout: string;
    puppyGender: string;
    puppyAge: string;
    puppyPrice: string;
}

export interface ParamsProps {
    params: {
        id: string;
    }
}